export enum TimelineItemType {
  CREATE = 'primary',
  CREATE_SUB_TASK = 'info',
  FINISH = 'success',
  UPDATE = 'warning',
  DELETE = 'error',
}

export enum TimelineItemIcon {
  CREATE = 'mdi-plus',
  CREATE_SUB_TASK = 'mdi-plus-circle',
  FINISH = 'mdi-check-underline',
  UPDATE = 'mdi-pencil',
  DELETE = 'mdi-delete',
}
